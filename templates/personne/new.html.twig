<!DOCTYPE html>

<title>New Personne</title>

{% block body %}
    <h1>Create new Personne</h1>

    {{ include('personne/_form.html.twig') }}

    <a href="{{ path('app_personne_index') }}">back to list</a>
{% endblock %}
